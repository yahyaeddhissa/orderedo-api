type Query {
  users: [User]
}

type Mutation {
  createUser(input: CreateUserInput): User
}

input CreateUserInput {
  name: String
  email: String
  username: String
}

type User {
  id: String
  username: String
  email: String
  name: String
  isVerified: String
  isMember: Boolean
}
