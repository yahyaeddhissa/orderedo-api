# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

interface Product {
  id: ID!
  name: String!
  shortDescription: String
  fullDescription: [FullDescriptionParagraph!]
  averageRating: Float
}

type User {
  id: ID!
  email: String
  name: String!
  isMember: Boolean!
}

type FullDescriptionParagraph {
  title: String!
  text: String!
}

type PendingProduct implements Product {
  id: ID!
  name: String!
  shortDescription: String
  fullDescription: [FullDescriptionParagraph!]
  averageRating: Float
  author: User!
}

type RejectedProduct implements Product {
  id: ID!
  name: String!
  shortDescription: String
  fullDescription: [FullDescriptionParagraph!]
  averageRating: Float
  author: User!
  rejectedBy: User!
}

type PublicProduct implements Product {
  id: ID!
  name: String!
  shortDescription: String
  fullDescription: [FullDescriptionParagraph!]
  averageRating: Float
  approvedBy: User!
}

type Query {
  review(id: String!): ReviewResult!
  productReviews(id: String!): [ReviewResult!]!
  product(id: String!): Product!
  users: [User!]!
}

union ReviewResult = PendingReview | PublicReview | RejectedReview

type PendingReview {
  id: ID!
  author: User!
  product: Product!
  rating: Float!
  content: String!
}

type PublicReview {
  id: ID!
  author: User!
  product: Product!
  rating: Float!
  content: String!
  approvedBy: User!
}

type RejectedReview {
  id: ID!
  author: User!
  product: Product!
  rating: Float!
  content: String!
  rejectedBy: User!
}

type Mutation {
  createProduct(input: ProductCreateInput!): Product!
  createUser(input: CreateUserInput!): User!
}

input ProductCreateInput {
  name: String!
  shortDescription: String!
  fullDescription: [FullDescriptionParagraphInput!]!
}

input FullDescriptionParagraphInput {
  title: String!
  text: String!
}

input CreateUserInput {
  firstName: String!
  lastName: String!
  email: String!
  password: String!
}