# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type User {
  id: ID!
  email: String!
  name: String!
  isVerified: Boolean!
  isMember: Boolean!
}

type ReviewProduct {
  id: String!
  name: String!
  slug: String!
}

type ReviewAuthor {
  id: ID!
  name: String!
  isVerified: Boolean!
  isMember: Boolean!
}

type ReviewAdmin {
  id: ID!
  name: String!
}

type ApprovedReview {
  id: ID!
  author: ReviewAuthor!
  product: ReviewProduct!
  rating: Float!
  content: String!
  approver: ReviewAdmin!
}

type Query {
  review(id: String!): ApprovedReview!
  productReviews(id: String!): [ApprovedReview!]!
  product: ProductResult!
  users: [User!]!
}

union ProductResult = PublicProduct | PendingProduct | RejectedProduct

type PublicProduct {
  id: ID!
  name: String!
  slug: String!
  shortDescription: String!
  fullDescription: String!
  averageRating: Float!
  approvedBy: User!
}

type PendingProduct {
  id: ID!
  name: String!
  slug: String!
  shortDescription: String!
  fullDescription: String!
  averageRating: Float!
  author: User!
}

type RejectedProduct {
  id: ID!
  name: String!
  slug: String!
  shortDescription: String!
  fullDescription: String!
  averageRating: Float!
  author: User!
  rejectedBy: User!
}

type Mutation {
  createUser(input: CreateUserInput!): User!
}

input CreateUserInput {
  firstName: String!
  lastName: String!
  email: String!
  password: String!
}